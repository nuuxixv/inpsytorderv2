# 개발 로드맵

> 이 파일은 향후 개발 방향과 미완료 기능을 기록합니다.

---

## 📍 현재 상태 (2026-04-07 기준)

| 구분 | 상태 |
|------|------|
| 서비스 단계 | **정식** — 알림톡 연동 완료 |
| 알림톡 | 결제완료(paid) 시 자동 발송, 어드민 재발송 버튼 |
| 이메일 수집 | 제거됨 (알림톡으로 대체) |
| RLS 보안 | ✅ 완료 — get_order_by_token RPC, anon SELECT 차단 |

### 정식 전환 완료 체크리스트
- [x] 알림톡 채널 심사 통과
- [x] 알림톡 API 연동 (원샷 msgagent, 결제완료 시 자동 발송)
- [x] 어드민 재발송 버튼 (`OrderDetailModal`)
- [x] 이메일 필드 제거 (주문 폼, DB nullable 처리)
- [x] 어드민 "링크 복사" 버튼 제거 (이미 없음 확인)
- [x] 주문 상태 페이지 배너 통합 (완료)
- [x] RLS DB function 수정 — get_order_by_token RPC, anon SELECT 차단

---

## 🔴 우선순위 높음 — 어드민 검수 이슈 (2026-04-08)

### [공통] UI/기능 이슈
- [x] 어드민 타이틀 "인싸이트 오더" → "인싸이트 현장주문"으로 변경
- [x] 사이드바/페이지 타이틀 로고 불일치 (대시보드·주문관리·상품관리·설정 전체)
- [x] 대시보드 학회 드롭다운 → 학회관리(주최 학회)와 데이터 동기화
- [x] 상세행사 선택 → 단일 드롭다운에서 체크박스 멀티 선택으로 변경
- [x] 시스템 접속 기록(최근 20건) 항상 비어있음 → "준비 중" 플레이스홀더로 교체
- [ ] 우측 상단 알림 로그 누적 뷰 추가

### [주문관리] UI/기능 이슈
- [ ] 전반적인 UX/UI 개선 (어드민 전체 포함)
- [x] 학회선택 드롭다운 → 체크박스 멀티 선택
- [x] 주문상태 필터 → 체크박스 멀티 선택
- [x] 필터 초기화 버튼 UI 통일 (RestartAltIcon + "초기화" 텍스트)
- [x] 신규 주문 추가 → 상품 검색/장바구니 방식으로 재설계 (NewOrderModal.jsx)

---

## 🔴 우선순위 높음

### [부분 취소] 기획 필요
고객이 특정 상품만 취소하는 경우 퍼널:
카드 전체 환불 → 취소 상품 제외한 2차 주문 생성 → 재결제
→ **기획 확정 후 구현**

---

## 🟡 중간 우선순위

### ✅ [주문 조회] 고객용 주문 상태 페이지 (구현 완료)
- 라우트: `/order/status/:token`
- UUID access_token 기반, 로그인 불필요
- 연계 주문(parent_order_id) 병합 표시
- 배송 예정일 상태별 문구 분기

### ✅ [배송 예정일] 학회별 예상도착일 (구현 완료)
- DB: `events.estimated_delivery_date date` 추가 완료
- 주문서 최종 확인 단계 + 주문 상태 페이지 표시

## 🟢 낮은 우선순위

### [출고 완료 알림톡] 추가 템플릿
현재 `paid` (결제완료) 시 발송. 추후 `shipped` (출고완료) 시 별도 알림톡 추가 가능.
템플릿 별도 등록 필요 (원샷 → 카카오 심사).

### [주문 조회 페이지] 이름+연락처 조회 (Fallback) — 보류
보안 취약하므로 구현하지 않기로 결정. 알림톡 재발송이 이 역할 대체.

---

## ✅ 완료된 주요 작업
- **RLS 보안 강화** (2026-04-07): anon SELECT 차단, get_order_by_token RPC, OrderLookupPage 제거
- **카카오 알림톡 연동** (2026-04-07): 원샷 msgagent API, 결제완료 자동 발송, 어드민 재발송 버튼
- **이메일 제거**: 주문 폼에서 이메일 수집 제거, DB nullable 처리
- **주문 상태 페이지 배너 통합**: 단일 카드로 정리
- **어드민 링크 복사 버튼 제거**: 베타 잔재 정리 완료
- 주문 상태 6단계로 단순화
- 어드민 전체 디자인 시스템 통일
- 대시보드 매출 계산 로직 수정 (배송비 분리, 취소/환불 제외)
- 출고현황 결제완료 전용 뷰
- 연계 주문 UI (주문관리 목록 포맷, 출고현황 병합, 주문상세 1차/2차 분리)
- 고객 주문 조회 페이지 (`/order/status/:token`) — UUID 토큰 기반
- 배송 예정일 (`events.estimated_delivery_date`)
- UUID access_token 도입 (순번 ID 열거 공격 차단)
- 어드민 주문 편집 배송비 계산 정가 기준 수정
- 상품관리 할인상품 카운트 수정
- 주문 검색 연락처 하이픈 무관 검색


